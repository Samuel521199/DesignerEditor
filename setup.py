from setuptools import setup, find_packages

setup(
    name="DesignerEditor",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "PyQt6",
    ],
    python_requires=">=3.8",
) 